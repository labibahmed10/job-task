import React from "react";
import TableContent from "../TableContent/TableContent";

const HomePage = () => {
  return (
    <section>
      <TableContent />
    </section>
  );
};

export default HomePage;
